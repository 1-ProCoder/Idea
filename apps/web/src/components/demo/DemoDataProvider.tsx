// Demo data provider was removed entirely.
//
// This module is intentionally an empty stub. The previous demo dataset
// (fictional customers, workers, jobs, appointments, calls) is gone —
// every dashboard page now reads from the live API via Clerk-authenticated
// fetch hooks. Keeping this file avoids any "module not found" errors
// from stale build caches or yarn pnp lookups.
export {};
