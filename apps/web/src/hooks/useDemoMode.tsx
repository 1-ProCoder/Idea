// Demo mode was removed entirely — see commit message.
//
// This module is intentionally an empty stub so any stale references
// (e.g. comments, cached bundle entries, or third-party search) resolve
// to a valid file without exporting the deprecated hook. All previous
// callers have been migrated off `useDemoMode`.
export {};
