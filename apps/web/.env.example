# Copy this file to apps/web/.env and fill in values from your Clerk dashboard.
# Re-start `npm run dev` after editing so Vite picks the new values up.

# Public Clerk key (safe to expose — starts with pk_test_… in dev, pk_live_… in prod)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace_me
